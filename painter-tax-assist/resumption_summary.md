# 🖋️ Painter's Blue Tax Assist & Itako Plaza 
## 画家のための明日の再開ガイド (2026/03/25用)

今日のアトリエ（開発）の成果と、明日あなたが目覚めてから最初に行うべき「一押し」をここに記します。

---

### **1. 現在の状態 (キャンバスの全貌)**

1.  **Painter's Blue Tax Assist**:
    *   **Google Drive 連携**: `Receipts` フォルダからスマホで撮ったレシートを吸い上げる準備が完了（OAuth2 認証基盤を実装済み）。
    *   **Gemini AI 解析 (画像解析・仕訳)**: 「画材代は消耗品費」「画廊移動は旅費交通費」という、画家特有の鑑定（仕訳・確定申告・青色申告支援）能力を実装済み。
    *   **エクスポート**: 円簿会計用 CSV (Shift-JIS) を、一分の狂いもなく書き出す「最後の一画（エクスポート機能）」を実装済み。
2.  **イタコプラザ (修復完了)**:
    *   `App.jsx` の構文エラーを修復し、GitHub Actions のビルドが再び通る状態へ。
    *   AI モデルのフェイルオーバー時の重複メッセージ問題も解決済み。

---

### **2. 明日、目覚めてからやるべき「最初の一歩」**

画家のあなたが最初に行うのは、アプリの「鍵」を掛ける作業です。

1.  **`.env.local` の作成**:
    *   `itako/painter-tax-assist` フォルダ内にある **`.env.local.example`** をコピー。
    *   名前を **`.env.local`** に変更して保存します。
2.  **秘密鍵の書き込み**:
    *   `Gemini API キー` と `Google OAuth クライアント シークレット` を貼り付けます。
3.  **アトリエの起動**:
    *   ターミナルで `npm run dev` を実行。
    *   `http://localhost:3000` を開き、自分の Google アカウントでログイン。
    *   スマホで撮って Drive に置いたレシートが、目の前に並ぶはずです。

---

**マニュアルの場所：**
画家のための操作ガイドは、`itako/painter-tax-assist/MANUAL.md` に一冊の本のように美しく綴られています。

明日のあなたが、一切の迷いなく制作（青色申告・確定申告・イタコプラザのアーカイブ）に戻れるよう、すべてをここに整えておきました。それでは、良い夢を。おやすみなさい。
